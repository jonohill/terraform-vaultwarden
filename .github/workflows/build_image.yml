name: build docker image

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

env:
  DOCKER_REGISTRY: ghcr.io/jonohill
  DOCKER_REPO: fly-vaultwarden

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to ghcr
        uses: docker/login-action@v2
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: (Pre) build image
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max          
          tags: "${{ env.DOCKER_REGISTRY}}/${{ env.DOCKER_REPO }}:${{ github.run_number }}-dev"

      - name: Get vaultwarden version
        id: vaultwarden_version
        run: |
          version=$(docker run --entrypoint /vaultwarden "${{ env.DOCKER_REGISTRY}}/${{ env.DOCKER_REPO }}:${{ github.run_number }}-dev" --version | grep -oE '\d+\.\d+\.\d+')
          echo "version=$version" >> $GITHUB_OUTPUT          

      - name: Build and push
        id: docker_build_push
        uses: docker/build-push-action@v3
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.ref == 'refs/heads/main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max          
          tags: |
            "${{ env.DOCKER_REGISTRY}}/${{ env.DOCKER_REPO }}:latest"
            "${{ env.DOCKER_REGISTRY}}/${{ env.DOCKER_REPO }}:${{ steps.vaultwarden_version.outputs.version }}"
